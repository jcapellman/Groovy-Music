<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GroovyMusic.Controls.ArtistView">

    <ContentView.Resources>
        <ResourceDictionary Source="../Styles/ArtistView.xaml"/>
    </ContentView.Resources>

    <ContentView.Content>
        <Grid>
            <ListView Style="{StaticResource ArtistView}" ItemsSource="{Binding Artists}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Grid>
                                <Label Style="{StaticResource ArtistName}"  Text="{Binding Name}" />
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
  </ContentView.Content>
</ContentView>